return {
  "slowy07/mywpm.nvim",
  opts = {
    -- config options
    -- 1 minute
    notify_interval = 60 * 1000,
    -- high wpm (default)
    high = 60,
    -- low wpm (default)
    low = 15,
    notify = false,
  }
}
